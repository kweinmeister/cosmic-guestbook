{
  "name": "cosmic-guestbook",
  "version": "1.0.0",
  "scripts": {
    "gcp-build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "node backend/server.js"
  }
}